/**
 * Reference width the density caps below are calibrated against. Per D001,
 * buildDensityZones always takes totalWidth as an explicit caller argument —
 * this is not a default value, just the fixture width tests use.
 */
export const VIRTUAL_CANVAS_WIDTH = 20000;

export const MIN_PX_PER_YEAR = 1;
export const MAX_PX_PER_YEAR = 20;
