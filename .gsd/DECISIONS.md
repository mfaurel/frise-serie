# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | Planning S02 (yearToPixel density algorithm) — M001 | architecture | Where does data/density.ts (precomputed zones) get built, and when in the roadmap? | S02 implements only the pure lib functions (lib/constants.ts, lib/density.ts, lib/yearToPixel.ts, lib/pixelToYear.ts) parameterized by an explicit totalWidth argument. data/density.ts (a precomputed export, per ARCHITECTURE.md's directory sketch) is deferred to whichever later UI slice first needs a concrete totalWidth. | totalWidth is a layout/viewport concern that no slice has established yet (no Timeline component exists). Precomputing zones against a guessed width now would bake in a number no consumer confirmed; buildDensityZones already takes totalWidth as a parameter per ARCHITECTURE.md's signature, so deferring the precomputed export costs nothing and avoids inventing UI-layer values inside a pure-algorithm slice. | true | agent |
| D002 | Planning S02 (yearToPixel density algorithm) — M001 | architecture | Which Show field feeds density placement (show count per era) in buildDensityZones? | Use Show.narrativeYearStart (the year the show's story is set in) to bucket a show into an era, not broadcastYearStart (the year it aired). | The timeline visualizes historical narrative time, not broadcast history; ARCHITECTURE.md's own S02 demo example (yearToPixel(793, zones) for "Vikings start") uses 793, which is Vikings' narrativeYearStart (793-865), not its broadcastYearStart (2013-2020). | true | agent |
| D003 |  | architecture | How do filters.ts predicates compose across region/genre/platform/accuracy, and what does an absent/empty filter value mean? | Each category (regions, genres, platforms) matches a Show if the show's corresponding array intersects the criteria array for that category (OR within category); an undefined or empty-array criteria value means "no filter for this category" (matches all shows). Accuracy uses a single optional minAccuracy: number threshold — a show matches if historicalAccuracyScore >= minAccuracy (undefined means no filter). All provided categories combine with AND. | No FilterPanel/nuqs wiring exists yet (deferred to a later UI slice per ARCHITECTURE.md), so filters.ts must define pure, self-consistent semantics now so a future slice can bind nuqs query-array/number params directly without re-deciding matching logic. Threshold-style accuracy (>=) matches PRD F-20's "filter by historical accuracy score" as a reliability floor, consistent with genre/region/platform being inclusion-set filters. | true | agent |
| D004 |  | architecture | Does yearToDisplay need to handle year 0 even though no current data uses it? | yearToDisplay(0, locale) returns 'Ier s.' (fr) / '1st c.' (en) per TECHNICAL.md section 13's explicit table row, and this is unit-tested even though data/eras.ts and data/shows.ts never currently contain year 0. | TECHNICAL.md section 13 explicitly specifies this mapping as part of the BC/AD formatting contract, and the project's "no year zero" historical convention makes year 0 a meaningful boundary edge case worth contract-testing regardless of current dataset coverage. | true | agent |
| D005 |  | architecture | Scroll axis for the timeline — horizontal vs vertical | Vertical scroll (overflow-y). yearToPixel values map to CSS `top`; the inner container uses `height: totalPx; width: 100%`. | V5-atlas-editorial (the agreed design reference) uses vertical scroll. The user explicitly chose vertical after comparing the two prototypes. TECHNICAL.md previously stated horizontal as a constraint, but the human decision overrides that note. | yes — could revisit for desktop-wide layouts | human |
| D006 |  | architecture | How to drive parallax scroll animation in TimelineContainer without causing React re-renders on every scroll event | Direct DOM mutation via useRef — scroll handler reads scrollLeft and sets style.transform directly on bgLayerRef.current and axisLayerRef.current without calling setState | useState on the scroll path triggers a full React re-render on every scroll frame (up to 60/s), causing layout thrash. Direct style.transform mutation is GPU-composited and bypasses React's reconciler entirely, sustaining 60fps. The only consequence is that these transforms are invisible to React's virtual DOM, which is acceptable because they are purely presentational and do not drive data flow. | if a future requirement needs scroll position in React state (e.g. to conditionally render or animate other components), add a separate throttled useState update alongside the direct DOM mutation | agent |
| D007 |  | architecture | TimelineContainer component type — Server Component vs Client Component | Convert to Client Component ('use client') in S03 to support scroll event handling for parallax | S02 used a Server Component because all data (zones, eras) is synchronously importable and no browser APIs were needed. S03 introduces scroll event listeners (onScroll) and DOM refs (useRef) which are browser-only APIs, requiring 'use client'. The data import pattern (synchronous import of shows/eras at module level) is unchanged — the Client Component still imports data statically, avoiding any fetch or hydration cost. | if TimelineContainer is later split into a Server Component wrapper that passes pre-computed zone data as props to a Client Component inner shell | agent |
