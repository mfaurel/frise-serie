# S01: ShowCard component with star-node glow and positioning

**Goal:** Render ShowCard components at correct yearToPixel horizontal positions inside the parallax-cards layer of TimelineContainer, each showing an era-colored star-node glow, poster image (with fallback), show title, and broadcast/narrative year dates.
**Demo:** Show cards appear at correct yearToPixel positions with era-colored star glow, poster placeholder, title, and dates

## Must-Haves

- At least one [data-testid="show-card"] element visible in the browser at the pixel offset computed by yearToPixel(show.narrativeYearStart, zones). Each card displays: era-colored circular star-node glow (box-shadow), poster img (or grey SVG placeholder on error), show title text, and both broadcast and narrative year range. npx playwright test e2e/timeline-s01.spec.ts exits 0.

## Proof Level

- This slice proves: integration — dev server renders real React components; browser-executable Playwright spec confirms card presence, positioning, and title content

## Integration Closure

Upstream surfaces consumed: lib/yearToPixel.ts (pixel math), data/shows.ts (show list), data/eras.ts (era color palettes), types/index.ts (Show/Era/DensityZone types), app/components/TimelineContainer.tsx (parallax-cards mount point). New wiring: ShowCard imported and rendered inside the parallax-cards div in TimelineContainer. What remains before M003 is end-to-end: swim-lane collision avoidance (S02), narrative span bars and constellation lines (S03).

## Verification

- Each card carries data-testid="show-card" for Playwright targeting. Era-color glow is inspectable via browser DevTools computed styles (box-shadow value). Missing poster URL degrades to an inline grey SVG data-URI placeholder, visible in the Network panel as a data: request. Card left-offset is derivable from the element's computed style.left vs yearToPixel output.

<tasks>
- [x] **T01**: Created ShowCard component with era-colored star-node glow, poster fallback, title, and year labels _(45m)_
  Why: The parallax-cards layer in TimelineContainer is currently empty — no visual show representation exists yet. This task creates the foundational ShowCard component that all of M003 builds upon.
  - Files: `app/components/ShowCard.tsx`
  - Verify: npm run typecheck
- [x] **T02**: ShowCard wired into TimelineContainer parallax-cards layer with yearToPixel positioning; 4-test Playwright spec passes _(30m)_
  Why: ShowCard exists as an isolated component but nothing renders it — it must be mounted inside the parallax-cards layer of TimelineContainer to appear on the timeline. An E2E spec gives the slice an executable stopping condition.
  - Files: `app/components/TimelineContainer.tsx`, `e2e/timeline-s01.spec.ts`
  - Verify: npx playwright test e2e/timeline-s01.spec.ts
</tasks>

## Files Likely Touched

- app/components/ShowCard.tsx
- app/components/TimelineContainer.tsx
- e2e/timeline-s01.spec.ts
