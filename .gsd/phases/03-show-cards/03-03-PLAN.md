# S03: Narrative span bars and constellation lines

**Goal:** Add SVG span bars showing each show's narrative time span and constellation lines connecting genre-related shows, with hover-triggered highlighting of connected shows and their lines.
**Demo:** Vikings shows a span bar from 793 to 1002. Lines connect tag-related shows. Hover highlights connections.

## Must-Haves

- npx playwright test e2e/timeline-s03.spec.ts exits 0 with all tests passing; npm run typecheck exits 0

## Proof Level

- This slice proves: integration — React SVG overlay renders computed positions from real layout engine output; browser-tested via Playwright against live dev server

## Integration Closure

Upstream: lib/swimLane.ts (LaidOutShow, CARD_HEIGHT, BASE_TOP), lib/yearToPixel.ts, data/shows.ts, data/eras.ts, types/index.ts. New wiring: ConstellationLayer SVG component imported into TimelineContainer; hoveredShowId state added to TimelineContainer; card wrappers gain onMouseEnter/onMouseLeave. What remains: nothing — this slice completes M003's full visual feature set (ShowCard + swim lanes + span bars + constellation lines).

## Verification

- SVG element [data-testid="constellation-layer"] inspectable in browser DevTools; span-bar rects carry data-show-id; constellation-line paths carry data-show-a and data-show-b for targeted querying. Failure is browser-visible (no SVG layer) and Playwright-detectable (spec exits non-zero).

<tasks>
- [x] **T01**: Added pure constellation-lines logic (SpanBarDatum, SegmentDatum, computeRelatedShows, computeSpanBars, computeConstellationSegments) with 11 passing unit tests _(45m)_
  Why: Span-bar geometry and show-relationship computation need isolated, testable pure functions before any React component can consume them. Co-locating logic in lib/ keeps the component thin and the math verifiable without a browser.
  - Files: `lib/constellationLines.ts`, `tests/constellation-lines.test.ts`
  - Verify: npx vitest run tests/constellation-lines.test.ts
- [x] **T02**: Built ConstellationLayer SVG component rendering span bars and constellation lines, wired hover state into TimelineContainer _(1h)_
  Why: The pure logic from T01 needs a React SVG rendering layer and hover state wiring in TimelineContainer before any browser-visible behavior exists. This task completes the full visual implementation.
  - Files: `app/components/ConstellationLayer.tsx`, `app/components/TimelineContainer.tsx`
  - Verify: npm run typecheck
- [x] **T03**: Added 4-test Playwright E2E spec covering constellation SVG presence, span-bar count, constellation-line count, and hover-triggered opacity change — all 9 tests pass _(45m)_
  Why: Browser-observable acceptance gate for S03 visual behavior. TypeScript typecheck alone does not confirm the SVG is rendered at runtime or that hover state changes visual attributes correctly.
  - Files: `e2e/timeline-s03.spec.ts`
  - Verify: npx playwright test e2e/timeline-s03.spec.ts
</tasks>

## Files Likely Touched

- lib/constellationLines.ts
- tests/constellation-lines.test.ts
- app/components/ConstellationLayer.tsx
- app/components/TimelineContainer.tsx
- e2e/timeline-s03.spec.ts
