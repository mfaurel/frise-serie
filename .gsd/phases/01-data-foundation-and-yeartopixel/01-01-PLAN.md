# S01: TypeScript types and static data files

**Goal:** Create TypeScript type definitions (Show, Era, HistoricalEvent, Genre, Region, Platform) matching the PRD/architecture data model, and populate static data files (shows.ts, eras.ts, events.ts) with real historical TV series data — ~50 shows across 6+ eras, 20+ historical events.
**Demo:** Import shows.ts and log all 50 shows with correct types — no compile errors

## Must-Haves

- Complete the planned slice outcomes.

## Verification

- Run the task and slice verification checks for this slice.

<tasks>
- [x] **T01**: Bootstrapped project with package.json (Next.js 16, Motion 12, Tailwind v4, nuqs, next-intl, Vitest 4.1), tsconfig.json (strict, bundler resolution, path aliases), and vitest.config.ts
  - Files: `package.json`, `tsconfig.json`, `vitest.config.ts`, `.gitignore`
  - Verify: npx tsc --noEmit
- [x] **T02**: Created types/index.ts with Show, Era, HistoricalEvent, Flashback, Genre, Region, Platform, DensityZone, and LocalizedString types matching the PRD data model
  - Files: `types/index.ts`
  - Verify: npx tsc --noEmit
- [x] **T03**: Populated data/shows.ts (53 shows), data/eras.ts (9 eras), and data/events.ts (34 events) with real historical TV series data spanning Antiquity to Contemporary, fully typed against types/index.ts.
  - Files: `data/shows.ts`, `data/eras.ts`, `data/events.ts`
  - Verify: npx tsc --noEmit
- [x] **T04**: Added 25 Vitest data-integrity tests covering quantity thresholds, referential integrity, BC-date handling, and negative/malformed-input guards for shows/eras/events.
  - Files: `tests/data-integrity.test.ts`
  - Verify: npx vitest run
</tasks>

## Files Likely Touched

- package.json
- tsconfig.json
- vitest.config.ts
- .gitignore
- types/index.ts
- data/shows.ts
- data/eras.ts
- data/events.ts
- tests/data-integrity.test.ts
