# S03: Year display formatting and filter utilities

**Goal:** Add lib/yearToDisplay.ts (BC/AD year formatting in FR/EN per TECHNICAL.md section 13, including the year-0 "1st century" edge case) and lib/filters.ts (pure predicate builders/composition for region, genre, platform, and historical-accuracy filtering per D003), both fully covered by Vitest, completing M001's pure-function contract layer alongside S01/S02.
**Demo:** yearToDisplay(-73, 'fr') returns '73 av. J.-C.', yearToDisplay(-73, 'en') returns '73 BC'

## Must-Haves

- yearToDisplay(-73, 'fr') === '73 av. J.-C.' and yearToDisplay(-73, 'en') === '73 BC' (roadmap demo); yearToDisplay correctly formats positive years (pass-through), BC years (both locales), and the year-0 edge case ('Ier s.' / '1st c.') per D004. filterShows (or equivalent) composes region/genre/platform/accuracy predicates per D003's AND-across-categories, OR-within-category, empty-means-no-filter semantics, verified against the real data/shows.ts dataset (53 shows) not mocks. All new code passes `npx tsc --noEmit` in strict mode. No regressions to the existing 25 data-integrity + 25 yearToPixel tests.

## Proof Level

- This slice proves: contract — pure function correctness proven by unit tests; no UI wiring exists yet (FilterPanel/nuqs binding deferred to a future UI milestone), so this slice does not prove end-to-end filtering or display behavior in the browser.

## Integration Closure

Upstream surfaces consumed: types/index.ts (Show, Region, Genre, Platform), data/shows.ts (real dataset for filter tests). No new runtime wiring introduced — yearToDisplay and filters remain standalone lib functions with no consumers yet, same posture as S02's yearToPixel/pixelToYear before any Timeline component exists. What remains before end-to-end usability: a future UI slice must build FilterPanel.tsx, bind nuqs query state to filters.ts's criteria shape, and call yearToDisplay from axis/label rendering — none of that is in scope here.

## Verification

- Run the task and slice verification checks for this slice.

<tasks>
- [x] **T01**: Added lib/yearToDisplay.ts formatting historical years to FR/EN display strings (BC/AD, year-0 century edge case) with 11 Vitest cases. _(30m)_
  Why: The timeline stores years as plain numbers with negative values for BC (no year zero, per TECHNICAL.md section 13) but nothing formats them for display yet. This is the last piece needed for axis labels and detail-panel dates in a future UI slice.
  - Files: `lib/yearToDisplay.ts`, `tests/year-to-display.test.ts`
  - Verify: npx vitest run tests/year-to-display.test.ts && npx tsc --noEmit
- [x] **T02**: Added lib/filters.ts with composable region/genre/platform/accuracy show predicates per D003, covered by 25 Vitest cases against the real 53-show dataset. _(45m)_
  Why: PRD F-17 through F-20 require filtering the show timeline by region, platform, genre, and historical-accuracy score. No FilterPanel/nuqs wiring exists yet (deferred to a future UI slice), so this task only needs to establish the pure predicate/composition logic that a later slice will bind to URL query state, per D003.
  - Files: `lib/filters.ts`, `tests/filters.test.ts`
  - Verify: npx vitest run tests/filters.test.ts && npx tsc --noEmit
</tasks>

## Files Likely Touched

- lib/yearToDisplay.ts
- tests/year-to-display.test.ts
- lib/filters.ts
- tests/filters.test.ts
