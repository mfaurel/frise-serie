# S01: Next.js project scaffold with App Router and static export

**Goal:** Install dependencies and create the Next.js App Router scaffold with static export configuration, Tailwind CSS v4, and a Playwright smoke test, so that `npm run build` produces static HTML in `out/` and `npm run dev` serves at localhost:3000.
**Demo:** npm run build produces static HTML in out/, dev server runs at localhost:3000

## Must-Haves

- npm run build exits 0 and produces out/index.html; npm run typecheck exits 0; npx playwright test e2e/smoke.spec.ts passes against the dev server at localhost:3000.

## Proof Level

- This slice proves: integration — npm run build and npx playwright test exercise real runtime paths, not stubs.

## Integration Closure

Upstream: package.json (already contains next, react, tailwindcss, motion, nuqs, @playwright/test TBD), tsconfig.json, lib/ (M001 pure functions unchanged), types/index.ts. New wiring introduced: next.config.ts (output: export), app/layout.tsx + app/page.tsx + app/globals.css (App Router entry), postcss.config.mjs (Tailwind v4 PostCSS plugin). What remains before milestone is usable: S02 adds the horizontal scroll container on top of this scaffold; S03 adds the parallax + era backgrounds.

## Verification

- Build output: inspect out/ directory tree after npm run build. Dev server: stdout logs on npm run dev, accessible at localhost:3000. TypeScript: npm run typecheck exits 0 or prints errors. Playwright: npx playwright test prints pass/fail per spec.

<tasks>
- [ ] **T01**: Scaffolded Next.js 16 App Router with static export, Tailwind CSS v4, and installed all dependencies — typecheck exits 0 _(45m)_
  **Why:** The project has package.json listing next, react, tailwindcss, motion, nuqs, and related devDependencies but no node_modules and no App Router structure. This task bootstraps the runnable Next.js app.
  - Files: `package.json`, `next.config.ts`, `postcss.config.mjs`, `app/globals.css`, `app/layout.tsx`, `app/page.tsx`
  - Verify: npm run typecheck
- [ ] **T02**: Static export build produces out/index.html and Playwright smoke test scaffold is in place _(30m)_
  **Why:** The slice demo requires two observable outcomes: (1) `npm run build` produces static HTML in `out/`, and (2) a Playwright smoke test confirms the dev server serves the page correctly. This task exercises the static export path and lays down the browser-testing infrastructure for all subsequent M002 slices.
  - Files: `playwright.config.ts`, `e2e/smoke.spec.ts`, `package.json`, `.gitignore`
  - Verify: test -f out/index.html
</tasks>

## Files Likely Touched

- package.json
- next.config.ts
- postcss.config.mjs
- app/globals.css
- app/layout.tsx
- app/page.tsx
- playwright.config.ts
- e2e/smoke.spec.ts
- .gitignore
