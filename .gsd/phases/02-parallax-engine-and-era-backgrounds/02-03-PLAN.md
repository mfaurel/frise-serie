# S03: 3-layer parallax with era nebula backgrounds

**Goal:** Convert TimelineContainer to a high-performance Client Component with 3 parallax layers (background 0.3x, axis 0.6x, cards 1.0x) and era-specific CSS gradient nebula backgrounds derived from each era's colorPalette array.
**Demo:** Scrolling shows background layer at 0.3x, axis at 0.6x, card layer at 1.0x. Each era has distinct nebula gradient colors.

## Must-Haves

- Scrolling the timeline shows background layer at 0.3x effective speed, axis labels at 0.6x, cards at 1.0x. Each of the 9 eras has a distinct gradient background. All S02 Playwright assertions still pass. New S03 spec passes: 3 parallax layers visible, 9 era-bg elements, scroll drives transform change. TypeScript compiles with 0 diagnostics.

## Proof Level

- This slice proves: Integration + UAT (runtime-executable via npx playwright test)

## Integration Closure

Upstream consumed: app/components/TimelineContainer.tsx (S02 output), data/eras.ts (colorPalette), lib/density.ts, lib/yearToPixel.ts, lib/constants.ts. New wiring: Client Component scroll handler that directly mutates parallax layer transforms via DOM refs (no React state on scroll path to avoid re-renders). What remains before milestone is end-to-end usable: show card rendering in M003.

## Verification

- Scroll events directly mutate parallax-bg and parallax-axis transform styles — computed style is inspectable live via browser DevTools Elements panel. No structured logging needed.

<tasks>
- [ ] **T01**: Converted TimelineContainer to a parallax Client Component with 3 absolutely-positioned layers and era-derived CSS gradient nebula backgrounds. _(1h)_
  Why: Parallax requires reading scrollLeft (a browser-only API), so the Server Component from S02 must become a Client Component. Era colorPalette arrays (4 hex stops each) drive per-era CSS gradient backgrounds. Direct DOM mutation via refs avoids React re-renders on each scroll event, keeping animation at 60fps.
  - Files: `app/components/TimelineContainer.tsx`
  - Verify: npm run typecheck
- [ ] **T02**: Created e2e/timeline-s03.spec.ts with 5 passing parallax assertions and fixed stale smoke.spec.ts heading check _(30m)_
  Why: The parallax implementation needs browser-observable verification. The S02 summary explicitly flagged that e2e/smoke.spec.ts expects a "Frise Série" heading that was removed when app/page.tsx was updated to render TimelineContainer — this must be fixed before the full suite is run in milestone validation.
  - Files: `e2e/timeline-s03.spec.ts`, `e2e/smoke.spec.ts`
  - Verify: npx playwright test e2e/timeline-s03.spec.ts --reporter=line
</tasks>

## Files Likely Touched

- app/components/TimelineContainer.tsx
- e2e/timeline-s03.spec.ts
- e2e/smoke.spec.ts
