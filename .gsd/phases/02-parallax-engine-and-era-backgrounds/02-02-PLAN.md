# S02: Horizontal scroll container with yearToPixel width

**Goal:** Build a horizontally-scrollable timeline container whose total pixel width is derived from buildDensityZones(shows, eras, VIRTUAL_CANVAS_WIDTH). Era-boundary year labels are absolutely positioned via yearToPixel. Wire the component into app/page.tsx so the timeline is visible on the home route.
**Demo:** Timeline container scrolls horizontally, total width matches sum of density zones, year axis labels appear at correct positions

## Must-Haves

- 1. npm run typecheck exits 0 with no errors. 2. npx playwright test e2e/timeline-s02.spec.ts exits 0: scroll container is visible, inner div offsetWidth > 10000, at least one year-label element is present.

## Proof Level

- This slice proves: integration — exercises real show/era data through buildDensityZones and yearToPixel in a live Next.js browser render

## Integration Closure

Upstream: lib/density.ts (buildDensityZones), lib/yearToPixel.ts (yearToPixel), lib/constants.ts (VIRTUAL_CANVAS_WIDTH), data/shows.ts, data/eras.ts, types/index.ts. New wiring: TimelineContainer server component composed into app/page.tsx. What remains: S03 adds 3-layer parallax with era nebula backgrounds on top of this container.

## Verification

- Browser DevTools: inspect [data-testid="timeline-inner"] offsetWidth to confirm computed pixel width. Playwright report at playwright-report/ on test failure. npm run typecheck surfaces TS errors with exact file and line.

<tasks>
- [x] **T01**: Created TimelineContainer server component: scrollable div sized to density-zone totalWidth with era year labels absolutely positioned via yearToPixel. _(45m)_
  Why: app/page.tsx currently shows only a centered heading. This task creates the timeline UI component — a horizontally-scrollable div whose pixel width equals the last density zone's pixelEnd, with era-boundary year labels absolutely positioned using yearToPixel.
  - Files: `app/components/TimelineContainer.tsx`
  - Verify: npm run typecheck
- [x] **T02**: Wired TimelineContainer into app/page.tsx, fixed horizontal scroll axis, and added 3-assertion Playwright e2e spec — all tests pass. _(30m)_
  Why: The component built in T01 must be rendered on the home route, and the slice acceptance criteria (scrollable container with correct pixel width, year labels visible) must be exercised by an automated browser test so slice closeout can use runtime-executable UAT.
  - Files: `app/page.tsx`, `e2e/timeline-s02.spec.ts`
  - Verify: npx playwright test e2e/timeline-s02.spec.ts
</tasks>

## Files Likely Touched

- app/components/TimelineContainer.tsx
- app/page.tsx
- e2e/timeline-s02.spec.ts
