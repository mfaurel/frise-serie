---
phase: 02-parallax-engine-era-backgrounds
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/parallaxFormula.test.ts
  - lib/noiseUri.test.ts
  - components/TimelineSkeleton.test.ts
autonomous: true
requirements:
  - TL-01
  - TL-03
  - UX-04

must_haves:
  truths:
    - "lib/parallaxFormula.test.ts exists and runs with vitest run (no import errors, tests listed)"
    - "lib/noiseUri.test.ts exists and runs with vitest run (no import errors, tests listed)"
    - "components/TimelineSkeleton.test.ts exists and runs with vitest run (no import errors, tests listed)"
    - "npx vitest run exits 0 — all existing 38 tests still pass plus the new stub tests"
  artifacts:
    - path: "lib/parallaxFormula.test.ts"
      provides: "RED test stubs for bgX formula math (TL-01)"
      contains: "TOTAL_WIDTH"
    - path: "lib/noiseUri.test.ts"
      provides: "RED test stubs for SVG noise URI encoding (TL-03)"
      contains: "%23"
    - path: "components/TimelineSkeleton.test.ts"
      provides: "RED test stubs for skeleton render coverage (UX-04)"
      contains: "@vitest-environment jsdom"
  key_links:
    - from: "lib/parallaxFormula.test.ts"
      to: "lib/yearToPixel.ts"
      via: "import TOTAL_WIDTH"
      pattern: "import.*TOTAL_WIDTH.*from.*yearToPixel"
    - from: "components/TimelineSkeleton.test.ts"
      to: "components/TimelineSkeleton.tsx"
      via: "import TimelineSkeleton"
      pattern: "import.*TimelineSkeleton"
---

<objective>
Create the three Wave 0 test stubs required by the validation strategy before any implementation
begins. These stubs define the expected behaviors as RED tests (failing or todo) that Wave 1–3
implementations will make GREEN.

Purpose: Nyquist compliance — every requirement (TL-01, TL-03, UX-04) has an automated test gate
that exists before the code it validates. Prevents shallow implementations that pass only because
no test exists.

Output: Three test files that integrate cleanly with the existing vitest suite (38 tests currently
passing) and enumerate the behaviors Wave 1–3 plans must satisfy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-parallax-engine-era-backgrounds/02-CONTEXT.md
@.planning/phases/02-parallax-engine-era-backgrounds/02-VALIDATION.md
</context>

<interfaces>
<!-- Key types and exports the executor needs. Extracted from codebase. -->

From lib/yearToPixel.ts (confirmed in Phase 1):
```typescript
export function yearToPixel(year: number): number
export function pixelToYear(px: number): number
export const TOTAL_WIDTH: number  // sum of all era pixel spans
export const TIMELINE_START: number
export const TIMELINE_END: number
```

From lib/yearToPixel.test.ts (vitest convention, exact project pattern):
```typescript
import { describe, it, expect } from 'vitest'
// Named imports only — NO globals (STATE.md decision 01-01)
// it.todo for unimplemented stubs — NEVER it.skip (STATE.md decision 01-01)
```

From vitest.config.mts (environment per file):
// Node environment is default.
// Component tests override with: // @vitest-environment jsdom  (first line of file)

From data/eras.ts:
export const ERAS: HistoricalEra[]  // each element has: id, yearStart, yearEnd, gradient, name, pixelsPerYear
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write parallaxFormula.test.ts and noiseUri.test.ts stubs</name>
  <files>lib/parallaxFormula.test.ts, lib/noiseUri.test.ts</files>

  <read_first>
    lib/yearToPixel.ts — read to confirm TOTAL_WIDTH export name and value
    lib/yearToPixel.test.ts — read to replicate exact import pattern and describe/it structure
    vitest.config.mts — confirm default environment (node) so no directive needed for these files
  </read_first>

  <behavior>
    parallaxFormula.test.ts — describe 'bgX parallax formula':
      - it: "at progress=1 with zoom=1, offset equals -(TOTAL_WIDTH * 1 * 0.7)" — import TOTAL_WIDTH from @/lib/yearToPixel; compute `progress * -(TOTAL_WIDTH * zoom * 0.7)` where progress=1, zoom=1; expect result toBe(-(TOTAL_WIDTH * 0.7))
      - it: "shouldReduceMotion=true produces end value 0" — compute `shouldReduceMotion ? 0 : -(TOTAL_WIDTH * 1 * 0.7)` with shouldReduceMotion=true; expect 0
      - it: "shouldReduceMotion=false produces end value -(TOTAL_WIDTH * zoom * 0.7)" — same formula with shouldReduceMotion=false, zoom=2; expect -(TOTAL_WIDTH * 2 * 0.7)
      - it.todo: "at progress=0.5, zoom=1, offset equals -(TOTAL_WIDTH * 0.5 * 0.7)"

    noiseUri.test.ts — describe 'NOISE_SVG_URI encoding':
      - it.todo: "contains %23noise (Firefox-compatible filter reference)" — will import NOISE_SVG_URI from @/lib/noiseConstants once extracted; expect(NOISE_SVG_URI).toContain('%23noise')
      - it.todo: "starts with url(\"data:image/svg+xml," — expect correct prefix
      - it.todo: "does not contain bare # character outside percent-encoding"
      NOTE: noiseUri tests are all it.todo for Wave 0. The constant does not exist yet — it will
      be exported from lib/noiseConstants.ts in Wave 1. The it.todo stubs define what Wave 1 must
      make pass without blocking Wave 0's vitest run.
  </behavior>

  <action>
    Create lib/parallaxFormula.test.ts:
    - First line: no directive (node environment is default per vitest.config.mts)
    - Import { describe, it, expect } from 'vitest' (named imports — no globals)
    - Import { TOTAL_WIDTH } from '@/lib/yearToPixel'
    - Three implemented it() tests for the bgX formula math (described in behavior block above)
    - One it.todo() for the progress=0.5 case
    - All test assertions use pure arithmetic — no Motion API, no DOM required
    - The formula under test: offset = -(TOTAL_WIDTH * zoom * 0.7) at scroll progress=1

    Create lib/noiseUri.test.ts:
    - First line: no directive (node environment)
    - Import { describe, it } from 'vitest'
    - All three cases as it.todo() — the NOISE_SVG_URI constant does not exist yet; Wave 1 will
      extract it to lib/noiseConstants.ts and un-todo these tests
    - Comment above each it.todo explaining what to import once the constant is available:
      // import { NOISE_SVG_URI } from '@/lib/noiseConstants'
  </action>

  <verify>
    <automated>npx vitest run lib/parallaxFormula.test.ts lib/noiseUri.test.ts</automated>
  </verify>

  <done>
    Both files exist. npx vitest run exits 0 for both files. parallaxFormula.test.ts reports 3
    passing tests. noiseUri.test.ts reports 3 todo tests and 0 failures. No TypeScript errors on
    the imports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write TimelineSkeleton.test.ts stub</name>
  <files>components/TimelineSkeleton.test.ts</files>

  <read_first>
    lib/yearToPixel.test.ts — exact vitest import pattern, describe/it structure
    data/eras.ts — confirm ERAS array shape (id, yearStart, yearEnd fields needed in test assertions)
    lib/yearToPixel.ts — confirm yearToPixel signature for assertion computation
    vitest.config.mts — confirm jsdom environment override comment syntax
  </read_first>

  <behavior>
    components/TimelineSkeleton.test.ts — describe 'TimelineSkeleton':
      - @vitest-environment jsdom directive on first line (component render test requires DOM)
      - it.todo: "renders ERAS.length era band divs" — will use @testing-library/react render;
        query all divs with absolute positioning; expect count to equal ERAS.length
      - it.todo: "each era band div has style.left equal to yearToPixel(era.yearStart)" —
        iterate rendered divs and check each div's style.left against yearToPixel(era.yearStart) in pixels
      - it.todo: "each era band div has correct width from yearToPixel span" —
        check style.width equals (yearToPixel(era.yearEnd) - yearToPixel(era.yearStart)) in pixels
      - it.todo: "axis placeholder div is present at top: 48" — find the h-px div with top:48
      - it.todo: "has role=status for accessibility"
      NOTE: All tests are it.todo — TimelineSkeleton.tsx does not exist yet (Wave 1 creates it).
      The component import line is commented out to prevent TypeScript error on missing module.
  </behavior>

  <action>
    Create components/TimelineSkeleton.test.ts:
    - First line: // @vitest-environment jsdom  (required for DOM rendering — STATE.md decision 01-01)
    - Import { describe, it } from 'vitest' (named imports, no globals)
    - Comment out the component import with a note: // import TimelineSkeleton from './TimelineSkeleton' — uncomment in Wave 1
    - All five it.todo stubs as described in behavior block
    - Each it.todo includes a comment explaining the assertion and which file provides the import
    - Imports for ERAS and yearToPixel are included (non-commented) since these files exist now
      and serve as documentation of what the test will use
  </action>

  <verify>
    <automated>npx vitest run components/TimelineSkeleton.test.ts</automated>
  </verify>

  <done>
    File exists. npx vitest run exits 0 for this file. Reports 5 todo tests, 0 failures. The
    @vitest-environment jsdom directive is the first line. The import for TimelineSkeleton is
    commented out.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test files → lib constants | Test files import TOTAL_WIDTH from yearToPixel.ts — static import, no runtime injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 2 — zero install commands |
</threat_model>

<verification>
After both tasks complete:

```
npx vitest run
```

Must exit 0. Must show 38 existing tests passing + 3 new passing (parallaxFormula) + 3 new todo
(noiseUri) + 5 new todo (TimelineSkeleton). Zero test failures. Zero TypeScript errors in the
new test files.
</verification>

<success_criteria>
- lib/parallaxFormula.test.ts: 3 passing assertions, 1 todo
- lib/noiseUri.test.ts: 3 todos, 0 failures
- components/TimelineSkeleton.test.ts: 5 todos, 0 failures
- Full suite: npx vitest run exits 0, 38 prior tests still green
- No new TypeScript errors (npx tsc --noEmit exits 0)
</success_criteria>

<output>
Create .planning/phases/02-parallax-engine-era-backgrounds/02-01-SUMMARY.md when done
</output>
