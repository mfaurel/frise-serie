---
phase: 02-parallax-engine-era-backgrounds
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - components/AxisLayer.tsx
  - components/ShowCard.tsx
autonomous: true
requirements:
  - TL-01

must_haves:
  truths:
    - "AxisLayer.tsx exists as a standalone 'use client' component receiving zoom, locale, ticks, currentYear as props"
    - "ShowCard.tsx imports yearToDisplay from '@/lib/yearToDisplay' — no longer from '@/lib/timeline'"
    - "npx vitest run exits 0 — no regressions from import changes"
    - "npx tsc --noEmit exits 0"
  artifacts:
    - path: "components/AxisLayer.tsx"
      provides: "Client component — axis ticks + labels extracted from Timeline.tsx"
      exports: ["default AxisLayer"]
      contains: "use client"
    - path: "components/ShowCard.tsx"
      provides: "Show card component with corrected import path"
      contains: "yearToDisplay"
  key_links:
    - from: "components/AxisLayer.tsx"
      to: "lib/yearToPixel.ts"
      via: "import yearToPixel"
      pattern: "import.*yearToPixel.*from.*yearToPixel"
    - from: "components/AxisLayer.tsx"
      to: "lib/yearToDisplay.ts"
      via: "import yearToDisplay"
      pattern: "import.*yearToDisplay.*from.*yearToDisplay"
    - from: "components/ShowCard.tsx"
      to: "lib/yearToDisplay.ts"
      via: "import yearToDisplay"
      pattern: "import.*yearToDisplay.*from.*yearToDisplay"
---

<objective>
Create AxisLayer.tsx by extracting the time-axis JSX from Timeline.tsx into a standalone client
component. Update ShowCard.tsx to import yearToDisplay directly from '@/lib/yearToDisplay'
instead of formatYear from '@/lib/timeline'.

Purpose: Separating the axis into its own component prepares the three-layer architecture for
Plan 04's Timeline.tsx refactor. Fixing ShowCard.tsx's import eliminates a dependency on the
deprecated shim before Plan 04 deletes it. Both tasks are independent and safe to execute in
this wave without touching Timeline.tsx.

Output: components/AxisLayer.tsx (new), components/ShowCard.tsx (import updated only).
Timeline.tsx is NOT modified in this plan — Plan 04 handles Timeline's full refactor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-parallax-engine-era-backgrounds/02-CONTEXT.md
@.planning/phases/02-parallax-engine-era-backgrounds/02-PATTERNS.md
@.planning/phases/02-parallax-engine-era-backgrounds/02-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types and exports the executor needs. Extracted from codebase. -->

From components/Timeline.tsx lines 170–194 (axis section being extracted):
The axis section in Timeline.tsx has this structure:
  <div className="absolute left-0 right-0" style={{ top: 48 }}>
    <div className="absolute h-px bg-stone-600/60" style={{ left: 0, right: 0, top: 16 }} />
    {ticks.map((year) => {
      const x = yearToPixel(year) * zoom;
      const isCentury = year % 100 === 0;
      const is500 = year % 500 === 0;
      return (
        <div key={year} className="absolute flex flex-col items-center" style={{ left: x, transform: 'translateX(-50%)' }}>
          <div className={`w-px ${isCentury ? 'h-4 bg-stone-400' : 'h-2 bg-stone-600'}`}
               style={{ marginTop: is500 ? 0 : isCentury ? 4 : 8 }} />
          {isCentury && (
            <span className={`text-xs mt-0.5 font-mono ${is500 ? 'text-amber-500 font-bold text-sm' : 'text-stone-500'}`}>
              {formatYear(year, locale)}
            </span>
          )}
        </div>
      );
    })}
  </div>

From lib/yearToDisplay.ts:
```typescript
export function yearToDisplay(year: number, locale: string): string
// Replaces formatYear from @/lib/timeline — same behavior, new canonical name
```

From types/index.ts (or @/types):
```typescript
type Locale = 'fr' | 'en'
```

From components/ShowCard.tsx line 5:
// Current: import { formatYear } from '@/lib/timeline';
// Target:  import { yearToDisplay } from '@/lib/yearToDisplay';
// All formatYear(x, locale) calls become yearToDisplay(x, locale) — same signature
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create AxisLayer.tsx client component</name>
  <files>components/AxisLayer.tsx</files>

  <read_first>
    components/Timeline.tsx — read lines 170–194 to extract the exact axis JSX verbatim;
    also read lines 1–11 to see current imports; read lines 113–118 for how ticks array is computed
    lib/yearToPixel.ts — confirm yearToPixel export name
    lib/yearToDisplay.ts — confirm yearToDisplay export name
    @/types — confirm Locale type path
  </read_first>

  <action>
    Create components/AxisLayer.tsx as a new file:
    - First line: 'use client'
    - Imports: yearToPixel from '@/lib/yearToPixel', yearToDisplay from '@/lib/yearToDisplay',
      type Locale from '@/types'
    - Props interface:
        interface Props {
          zoom: number;
          locale: Locale;
          ticks: number[];
          currentYear: number;
        }
    - Component: export default function AxisLayer({ zoom, locale, ticks, currentYear }: Props)
    - Component body: return the axis JSX extracted verbatim from Timeline.tsx lines 170–194
      with these two changes:
        (a) formatYear(year, locale) → yearToDisplay(year, locale) in the tick label span
        (b) The AxisLayer does not render the current-year indicator pill — that stays in Timeline.tsx
    - The outer div is: className="absolute left-0 right-0" style={{ top: 48 }}
    - Axis line: className="absolute h-px bg-stone-600/60" style={{ left: 0, right: 0, top: 16 }}
    - Tick marks: iterate ticks prop, compute x = yearToPixel(year) * zoom, apply isCentury/is500
      logic and tick height classes exactly as they appear in Timeline.tsx
    - Year labels: only on century ticks; use yearToDisplay(year, locale); amber-500 on 500-year marks
    - currentYear prop is accepted but NOT rendered in this component — it is reserved for a
      future currentYear indicator on the axis (Phase 6 scope); accept the prop to keep the
      interface stable; add a comment: // TODO: render current-year marker in Phase 6
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <done>
    components/AxisLayer.tsx exists. Contains 'use client' on line 1. Imports yearToDisplay from
    '@/lib/yearToDisplay' (not formatYear from timeline). Props interface accepts zoom, locale,
    ticks, currentYear. npx tsc --noEmit exits 0 with no errors on this file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ShowCard.tsx imports (formatYear → yearToDisplay)</name>
  <files>components/ShowCard.tsx</files>

  <read_first>
    components/ShowCard.tsx — read the full file; identify all formatYear(...) call sites
    lib/yearToDisplay.ts — confirm yearToDisplay has the same signature as formatYear:
      yearToDisplay(year: number, locale: string): string
    lib/timeline.ts — confirm formatYear is a re-export alias for yearToDisplay (no behavior diff)
  </read_first>

  <action>
    In components/ShowCard.tsx:
    - Replace line 5: import { formatYear } from '@/lib/timeline'
      with: import { yearToDisplay } from '@/lib/yearToDisplay'
    - Replace all occurrences of formatYear( with yearToDisplay( in the component body
      (per PATTERNS.md, there is one usage: formatYear(show.narrativeYearStart, locale) and
      formatYear(show.narrativeYearEnd, locale) in the info strip section)
    - No other changes to ShowCard.tsx — styling, logic, other imports are untouched
    - After the edit, verify with grep that '@/lib/timeline' no longer appears in ShowCard.tsx
  </action>

  <verify>
    <automated>npx vitest run && npx tsc --noEmit</automated>
  </verify>

  <done>
    components/ShowCard.tsx imports yearToDisplay from '@/lib/yearToDisplay'. No reference to
    '@/lib/timeline' or 'formatYear' remains in ShowCard.tsx. npx vitest run exits 0. npx tsc
    --noEmit exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AxisLayer props → DOM render | tick years come from ERAS constant — static data, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks:

```
npx tsc --noEmit
```

Must exit 0. No type errors in AxisLayer.tsx or ShowCard.tsx.

```
npx vitest run
```

Must exit 0 — all existing tests still green. ShowCard.tsx import change has no test impact
(unit tests for ShowCard are Phase 3 scope).

Manual grep check:
- grep '@/lib/timeline' components/ShowCard.tsx — must return empty
- grep 'formatYear' components/ShowCard.tsx — must return empty
</verification>

<success_criteria>
- components/AxisLayer.tsx: 'use client' directive, Props interface with zoom/locale/ticks/currentYear,
  imports from @/lib/yearToPixel and @/lib/yearToDisplay only
- components/ShowCard.tsx: imports yearToDisplay from '@/lib/yearToDisplay', no formatYear, no @/lib/timeline
- npx tsc --noEmit exits 0
- npx vitest run exits 0 (no regressions)
</success_criteria>

<output>
Create .planning/phases/02-parallax-engine-era-backgrounds/02-03-SUMMARY.md when done
</output>
