---
phase: 01-data-foundation-yeartopixel
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - types/index.ts
  - data/eras.ts
autonomous: true
requirements:
  - TL-02

must_haves:
  truths:
    - "HistoricalEra interface has a pixelsPerYear field"
    - "All 9 eras in data/eras.ts have pixelsPerYear values summing to 9135px total timeline width"
    - "data/eras.ts last era (20th_century_late) ends at 2025 (not 1991)"
    - "PIXELS_PER_YEAR, TIMELINE_START, TIMELINE_END constants are removed from data/eras.ts"
    - "npx tsc --noEmit exits 0 after both files are updated"
  artifacts:
    - path: "types/index.ts"
      provides: "HistoricalEra interface with pixelsPerYear: number field"
      contains: "pixelsPerYear: number"
    - path: "data/eras.ts"
      provides: "9 eras with pixelsPerYear values, yearEnd: 2025 on last era"
      contains: "pixelsPerYear: 1.0"
  key_links:
    - from: "data/eras.ts"
      to: "types/index.ts"
      via: "import type { HistoricalEra } from '@/types'"
      pattern: "pixelsPerYear"
---

<objective>
Update the TypeScript interface and era data to support the non-linear piecewise scale. Two files change: `types/index.ts` gains `pixelsPerYear: number` on `HistoricalEra`, and `data/eras.ts` gains per-era `pixelsPerYear` values, a corrected `yearEnd` on the last era, and removes the now-obsolete linear-scale constants.

Purpose: These changes unblock Wave 2 — the yearToPixel implementation needs `HistoricalEra.pixelsPerYear` to exist before it can be written without TypeScript errors.

Output: Updated types/index.ts and data/eras.ts, both TypeScript-clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation-yeartopixel/01-CONTEXT.md
@.planning/phases/01-data-foundation-yeartopixel/01-RESEARCH.md
@.planning/phases/01-data-foundation-yeartopixel/01-PATTERNS.md

<interfaces>
From types/index.ts — HistoricalEra interface (current, lines 77–86):
```typescript
export interface HistoricalEra {
  id: string;
  name: { fr: string; en: string };
  yearStart: number;
  yearEnd: number;
  gradient: [string, string];
  textColor: string;
  description: { fr: string; en: string };
  keyEvents: HistoricalEvent[];
}
```
Change: add `pixelsPerYear: number;` after `yearEnd: number;`.

From data/eras.ts — obsolete constants to remove (lines 163-165):
  export const TIMELINE_START = -100;
  export const TIMELINE_END = 2000;
  export const PIXELS_PER_YEAR = 0.8;

From data/eras.ts — last era to update (lines 141-156):
  id: '20th_century_late', yearEnd: 1991 → change to 2025

Verified pixelsPerYear values (RESEARCH.md, total = 9135px):
  antiquity:           1.0  (span 3476yr × 1.0  = 3476px)
  early_middle_ages:   2.5  (span 524yr  × 2.5  = 1310px)
  middle_ages:         3.0  (span 400yr  × 3.0  = 1200px)
  renaissance:         5.0  (span 200yr  × 5.0  = 1000px)
  early_modern:        4.0  (span 189yr  × 4.0  =  756px)
  revolution_empire:   8.0  (span 26yr   × 8.0  =  208px)
  19th_century:        5.0  (span 85yr   × 5.0  =  425px)
  20th_century_early:  8.0  (span 45yr   × 8.0  =  360px)
  20th_century_late:   5.0  (span 80yr   × 5.0  =  400px)  ← span = 2025 - 1945
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pixelsPerYear to HistoricalEra interface</name>
  <files>types/index.ts</files>
  <read_first>
    - types/index.ts — read the full file before editing; specifically the HistoricalEra interface at lines 77–86
  </read_first>
  <action>
    In `types/index.ts`, add `pixelsPerYear: number;` to the `HistoricalEra` interface, positioned after the `yearEnd: number;` line and before the `gradient` line. The interface after the change must be:

    export interface HistoricalEra {
      id: string;
      name: { fr: string; en: string };
      yearStart: number;
      yearEnd: number;
      pixelsPerYear: number;
      gradient: [string, string];
      textColor: string;
      description: { fr: string; en: string };
      keyEvents: HistoricalEvent[];
    }

    No other changes to types/index.ts. Do not modify Show, Flashback, Genre, Region, Platform, FilterState, HistoricalEvent, or Locale. This is a single-line addition only.

    After editing, note that data/eras.ts will immediately produce TypeScript errors (the 9 era objects are missing the new required field) — this is expected and will be fixed in Task 2 of this plan.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "pixelsPerYear"</automated>
  </verify>
  <acceptance_criteria>
    - types/index.ts contains the line `pixelsPerYear: number;` inside the HistoricalEra interface (grep "pixelsPerYear" types/index.ts returns a match)
    - The line appears between `yearEnd: number;` and `gradient: [string, string];` (read file to confirm order)
    - No other interfaces in types/index.ts were modified
    - `npx tsc --noEmit` output contains errors mentioning "pixelsPerYear" and "HistoricalEra" — confirming the field was added and TypeScript is enforcing it on the data/eras.ts objects (these errors disappear after Task 2)
  </acceptance_criteria>
  <done>HistoricalEra interface has pixelsPerYear: number field. TypeScript reports errors on data/eras.ts objects that are missing this field — correct at this intermediate state.</done>
</task>

<task type="auto">
  <name>Task 2: Add pixelsPerYear values to all 9 eras and remove obsolete constants</name>
  <files>data/eras.ts</files>
  <read_first>
    - data/eras.ts — read the full file before editing; all 9 era objects plus the constant exports at lines 163-165
    - types/index.ts — confirm pixelsPerYear: number was added (Task 1 must complete first)
  </read_first>
  <action>
    In `data/eras.ts`, make three categories of changes:

    CHANGE 1 — Add `pixelsPerYear` field to each era object, after `yearEnd` and before `gradient`. Use exactly these values (per D-01, D-02, verified in RESEARCH.md to total 9135px):
    - antiquity:           pixelsPerYear: 1.0
    - early_middle_ages:   pixelsPerYear: 2.5
    - middle_ages:         pixelsPerYear: 3.0
    - renaissance:         pixelsPerYear: 5.0
    - early_modern:        pixelsPerYear: 4.0
    - revolution_empire:   pixelsPerYear: 8.0
    - 19th_century:        pixelsPerYear: 5.0
    - 20th_century_early:  pixelsPerYear: 8.0
    - 20th_century_late:   pixelsPerYear: 5.0

    CHANGE 2 — Fix the last era `20th_century_late`: change `yearEnd: 1991` to `yearEnd: 2025`. Also update its description name (the `name` field) if desired — the era currently uses `name: { fr: 'Guerre Froide', en: 'Cold War' }` but its new span runs to 2025; leave the name as-is (Cold War era name is acceptable for the full 1945–2025 range; renaming is Phase 9 scope). Do not change the name.

    CHANGE 3 — Remove the three obsolete export constants at the bottom of the file (lines 163-165):
    - Remove: `export const TIMELINE_START = -100;`
    - Remove: `export const TIMELINE_END = 2000;`
    - Remove: `export const PIXELS_PER_YEAR = 0.8;`

    Keep `export function getEraForYear(year: number): HistoricalEra | undefined { ... }` — this utility is still used.

    After these changes, `lib/timeline.ts` will produce a TypeScript error because it imports `TIMELINE_START`, `TIMELINE_END`, and `PIXELS_PER_YEAR` from `@/data/eras`. This is expected — lib/timeline.ts will be fixed in Wave 4 (01-05). For now, the TypeScript error in lib/timeline.ts is acceptable. The goal of this task's verification is that data/eras.ts itself has no errors.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "timeline.ts" | grep "error"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 era objects in data/eras.ts contain `pixelsPerYear:` field (grep -c "pixelsPerYear" data/eras.ts returns 9)
    - `20th_century_late` era has `yearEnd: 2025` (grep "yearEnd: 2025" data/eras.ts returns a match)
    - `TIMELINE_START`, `TIMELINE_END`, `PIXELS_PER_YEAR` are absent from data/eras.ts (grep "PIXELS_PER_YEAR" data/eras.ts returns no match)
    - `getEraForYear` function is still present (grep "getEraForYear" data/eras.ts returns a match)
    - `npx tsc --noEmit` errors are ONLY from lib/timeline.ts (which imports the now-removed constants) — data/eras.ts itself has zero TypeScript errors
    - Sum check: 3476 + 1310 + 1200 + 1000 + 756 + 208 + 425 + 360 + 400 = 9135 (mental verification — pixelsPerYear × span per era)
  </acceptance_criteria>
  <done>data/eras.ts has pixelsPerYear on all 9 eras, yearEnd: 2025 on last era, and no obsolete linear-scale constants. TypeScript errors remain only in lib/timeline.ts (addressed in Wave 4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static data modification | No external I/O — pure TypeScript constant changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | data/eras.ts pixelsPerYear values | accept | Values are static TypeScript constants, not user input. No runtime injection vector. |
</threat_model>

<verification>
After both tasks:
1. `grep -c "pixelsPerYear" data/eras.ts` returns 9
2. `grep "yearEnd: 2025" data/eras.ts` returns a match
3. `grep "PIXELS_PER_YEAR" data/eras.ts` returns no match
4. `grep "pixelsPerYear" types/index.ts` returns a match
5. `npx tsc --noEmit 2>&1 | grep -v "timeline.ts"` returns no error lines (all remaining TS errors are in lib/timeline.ts only)
</verification>

<success_criteria>
Wave 1 complete when:
- HistoricalEra interface includes pixelsPerYear: number
- All 9 eras have pixelsPerYear values (total: 9135px)
- Last era ends at 2025
- Old linear constants removed from data/eras.ts
- TypeScript errors exist only in lib/timeline.ts (expected — fixed in Wave 4)
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-yeartopixel/01-02-SUMMARY.md` when done.
</output>
