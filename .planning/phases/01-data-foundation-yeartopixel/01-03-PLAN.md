---
phase: 01-data-foundation-yeartopixel
plan: 03
type: tdd
wave: 2
depends_on:
  - 01-02
files_modified:
  - lib/yearToPixel.ts
  - lib/yearToDisplay.ts
  - lib/yearToPixel.test.ts
  - lib/yearToDisplay.test.ts
autonomous: true
requirements:
  - TL-02

must_haves:
  truths:
    - "yearToPixel(-3000) === 0"
    - "yearToPixel produces denser px/yr for renaissance (5.0) than antiquity (1.0)"
    - "Math.round(pixelToYear(yearToPixel(y))) === y for all 15 test years including -3000"
    - "yearToDisplay(0, 'en') === '1 BC' (year-0 display guard)"
    - "yearToDisplay(-52, 'fr') === '52 av. J.-C.'"
    - "npx tsc --noEmit has zero errors after lib/timeline.ts-related errors are excluded"
  artifacts:
    - path: "lib/yearToPixel.ts"
      provides: "Piecewise yearToPixel + pixelToYear + TOTAL_WIDTH + TIMELINE_START + TIMELINE_END"
      exports: ["yearToPixel", "pixelToYear", "TOTAL_WIDTH", "TIMELINE_START", "TIMELINE_END"]
    - path: "lib/yearToDisplay.ts"
      provides: "yearToDisplay(year, locale) with year-0 guard"
      exports: ["yearToDisplay"]
    - path: "lib/yearToPixel.test.ts"
      provides: "Full test suite — all it.todo entries replaced with real assertions"
      contains: "describe('pixelToYear round-trip"
    - path: "lib/yearToDisplay.test.ts"
      provides: "Full test suite for yearToDisplay"
      contains: "treats year 0 as 1 BC"
  key_links:
    - from: "lib/yearToPixel.ts"
      to: "data/eras.ts"
      via: "import { ERAS } from '@/data/eras'"
      pattern: "ERAS\\[0\\]\\.yearStart"
    - from: "lib/yearToPixel.test.ts"
      to: "lib/yearToPixel.ts"
      via: "relative import './yearToPixel'"
      pattern: "from './yearToPixel'"
---

<objective>
Implement the piecewise-linear yearToPixel function and the yearToDisplay formatter — with tests written first (TDD). Replace the stub it.todo entries in both test files with real RED assertions, then implement until GREEN.

Purpose: This is the mathematical backbone of the entire timeline. yearToPixel is load-bearing — every card position in Phases 2-9 depends on this function being correct. Unit tests lock in correctness before any visual work.

Output: lib/yearToPixel.ts, lib/yearToDisplay.ts, passing full test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation-yeartopixel/01-CONTEXT.md
@.planning/phases/01-data-foundation-yeartopixel/01-RESEARCH.md
@.planning/phases/01-data-foundation-yeartopixel/01-PATTERNS.md
@.planning/phases/01-data-foundation-yeartopixel/01-VALIDATION.md

<interfaces>
From data/eras.ts (after Wave 1 changes):
- export const ERAS: HistoricalEra[] (9 entries, each with pixelsPerYear field)
- ERAS[0].yearStart = -3000, ERAS[8].yearEnd = 2025
- Total width = sum of (era.yearEnd - era.yearStart) * era.pixelsPerYear = 9135px

From types/index.ts (after Wave 1 changes):
- export interface HistoricalEra { ..., pixelsPerYear: number, ... }

Expected exports from lib/yearToPixel.ts:
- export function yearToPixel(year: number): number
- export function pixelToYear(px: number): number
- export const TOTAL_WIDTH: number   (= 9135)
- export const TIMELINE_START: number  (= -3000, derived from ERAS[0].yearStart — D-04)
- export const TIMELINE_END: number    (= 2025, derived from ERAS[8].yearEnd — D-04)

Expected exports from lib/yearToDisplay.ts:
- export function yearToDisplay(year: number, locale?: 'fr' | 'en'): string
  (locale defaults to 'fr')

Key boundary pixel values (verified in RESEARCH.md):
- yearToPixel(-3000) = 0
- yearToPixel(476)   = 3476   (end of antiquity)
- yearToPixel(476)   = yearToPixel(476) from early_middle_ages start = 3476
- yearToPixel(1000)  = 4786
- yearToPixel(1400)  = 5986
- yearToPixel(1600)  = 6986
- yearToPixel(1789)  = 7742
- yearToPixel(1815)  = 7950
- yearToPixel(1900)  = 8375
- yearToPixel(1945)  = 8735
- yearToPixel(2025)  = 9135 = TOTAL_WIDTH
</interfaces>
</context>

<feature>
  <name>Piecewise yearToPixel + yearToDisplay</name>
  <files>lib/yearToPixel.ts, lib/yearToDisplay.ts, lib/yearToPixel.test.ts, lib/yearToDisplay.test.ts</files>
  <behavior>
    yearToPixel:
    - yearToPixel(-3000) → 0 (exactly)
    - yearToPixel(2025) → 9135 = TOTAL_WIDTH (exactly)
    - 1 year in renaissance (1400-1401) produces 5.0px; 1 year in antiquity (-1000 to -999) produces 1.0px
    - yearToPixel(476) from antiquity side equals yearToPixel(476) from early_middle_ages side (no jump at boundary)
    - yearToPixel(-9999) → 0 (clamp, no extrapolation)
    - yearToPixel(9999) → TOTAL_WIDTH (clamp, no extrapolation)
    - TIMELINE_START is derived from ERAS[0].yearStart (not hardcoded -3000) — D-04

    pixelToYear round-trip (D-13):
    - Math.round(pixelToYear(yearToPixel(y))) === y for all of: -3000, -1000, -500, -52, 1, 476, 793, 1066, 1492, 1600, 1789, 1815, 1900, 1945, 2024

    yearToDisplay:
    - yearToDisplay(-52, 'fr') → '52 av. J.-C.'
    - yearToDisplay(-52, 'en') → '52 BC'
    - yearToDisplay(-3000, 'en') → '3000 BC'
    - yearToDisplay(0, 'en') → '1 BC'  (year-0 display guard — D-06)
    - yearToDisplay(0, 'fr') → '1 av. J.-C.'
    - yearToDisplay(-1, 'en') → '1 BC'
    - yearToDisplay(1, 'en') → '1' (no AD suffix)
    - yearToDisplay(1789, 'fr') → '1789'
    - yearToDisplay(-52) → '52 av. J.-C.' (default locale = 'fr')
  </behavior>
  <implementation>
    RED phase — update test stubs to real assertions:
    1. Open lib/yearToPixel.test.ts. Replace each `it.todo(...)` with a real `it(...)` with a concrete `expect()`. Use `Math.round()` for pixelToYear round-trip assertions (not strict ===) per RESEARCH.md Pitfall 3. Keep the @/ alias import for ERAS.
    2. Open lib/yearToDisplay.test.ts. Replace each `it.todo(...)` with a real `it(...)` with concrete string assertions.
    3. Run `npx vitest run lib/yearToPixel.test.ts lib/yearToDisplay.test.ts` — all new assertions should FAIL (RED) because the implementation files don't exist.

    GREEN phase — create implementation files:
    4. Create lib/yearToDisplay.ts first (no dependencies on yearToPixel):
       - No imports needed
       - Export `yearToDisplay(year: number, locale: 'fr' | 'en' = 'fr'): string`
       - Apply year-0 guard: `const displayYear = year === 0 ? -1 : year`
       - If `displayYear < 0`: return locale-specific BC string using `Math.abs(displayYear)`
       - Otherwise: return `${displayYear}`
    5. Create lib/yearToPixel.ts:
       - Import: `import { ERAS } from '@/data/eras'`
       - Import type: `import type { HistoricalEra } from '@/types'`
       - Internal `buildOffsets(eras: HistoricalEra[]): number[]` — iterate eras, push cumulative, push total sentinel
       - Module-level `const ERA_OFFSETS = buildOffsets(ERAS)`
       - Export `TOTAL_WIDTH = ERA_OFFSETS[ERA_OFFSETS.length - 1]`
       - Export `TIMELINE_START = ERAS[0].yearStart` (derived — D-04)
       - Export `TIMELINE_END = ERAS[ERAS.length - 1].yearEnd` (derived — D-04)
       - Export `yearToPixel(year)`: loop ERAS with inclusive boundary `year >= era.yearStart && year <= era.yearEnd`; return `ERA_OFFSETS[i] + (year - era.yearStart) * era.pixelsPerYear`; clamp out-of-range
       - Export `pixelToYear(px)`: loop ERAS checking `px <= ERA_OFFSETS[i + 1]`; return `ERAS[i].yearStart + (px - ERA_OFFSETS[i]) / ERAS[i].pixelsPerYear`; return last era end if beyond range
    6. Run `npx vitest run lib/yearToPixel.test.ts lib/yearToDisplay.test.ts` — all assertions should PASS (GREEN).

    REFACTOR phase (if needed):
    7. Ensure JSDoc comments on yearToPixel and pixelToYear document the BC convention and clamping behavior.
    8. Run tests once more to confirm still GREEN.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ERAS data → yearToPixel | ERAS is a static TypeScript constant — no runtime injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | yearToPixel out-of-range inputs | mitigate | Clamp behavior: years before -3000 → 0px, years after 2025 → TOTAL_WIDTH. No extrapolation. Tested in lib/yearToPixel.test.ts clamp cases. |
| T-03-02 | Information Disclosure | yearToDisplay year 0 | accept | Year 0 display guard prevents unexpected "0" display; no security impact (pure formatting). Covered by test. |
</threat_model>

<verification>
After RED-GREEN-REFACTOR cycle:
1. `npx vitest run lib/yearToPixel.test.ts lib/yearToDisplay.test.ts` — all tests pass, zero failures
2. `npx tsc --noEmit 2>&1 | grep -v "timeline.ts" | grep "error TS"` — returns no lines (no errors outside lib/timeline.ts)
3. `grep "TIMELINE_START = ERAS\[0\]" lib/yearToPixel.ts` — returns a match (D-04 compliance: TIMELINE_START derived, not hardcoded)
4. `grep "PIXELS_PER_YEAR\|TIMELINE_START.*=.*-3000\|TIMELINE_END.*=.*2025" lib/yearToPixel.ts` — returns NO match for hardcoded values
</verification>

<success_criteria>
Wave 2 complete when:
- npx vitest run exits 0 with all yearToPixel and yearToDisplay tests passing
- TOTAL_WIDTH === 9135 (asserted by the end-of-timeline test)
- Round-trip holds for all 15 test years in D-13
- year-0 guard works: yearToDisplay(0, 'en') === '1 BC'
- TypeScript is clean for lib/yearToPixel.ts and lib/yearToDisplay.ts
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-yeartopixel/01-03-SUMMARY.md` when done.
</output>
