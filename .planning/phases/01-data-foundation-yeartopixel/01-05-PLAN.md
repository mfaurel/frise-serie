---
phase: 01-data-foundation-yeartopixel
plan: 05
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
files_modified:
  - lib/timeline.ts
  - CLAUDE.md
autonomous: true
requirements:
  - TL-02

must_haves:
  truths:
    - "lib/timeline.ts is a re-export shim — no implementation logic"
    - "Existing import `import { yearToPixel } from '@/lib/timeline'` still resolves without errors"
    - "CLAUDE.md stack line says Next.js 14.2, Framer Motion 11, Tailwind CSS 3, next-intl 3"
    - "CLAUDE.md BC dates line says 'negative = BC, direct negation: -52 = 52 BC'"
    - "npx tsc --noEmit exits 0 (all files clean)"
    - "npm run test:run exits 0"
  artifacts:
    - path: "lib/timeline.ts"
      provides: "Re-export shim — all exports delegate to lib/yearToPixel.ts or lib/yearToDisplay.ts"
      contains: "from './yearToPixel'"
    - path: "CLAUDE.md"
      provides: "Correct stack versions and BC dates constraint"
      contains: "Next.js 14.2"
  key_links:
    - from: "lib/timeline.ts"
      to: "lib/yearToPixel.ts"
      via: "re-export: export { yearToPixel, pixelToYear, TOTAL_WIDTH, TIMELINE_START, TIMELINE_END } from './yearToPixel'"
      pattern: "from './yearToPixel'"
    - from: "lib/timeline.ts"
      to: "lib/yearToDisplay.ts"
      via: "re-export: export { yearToDisplay as formatYear } from './yearToDisplay'"
      pattern: "from './yearToDisplay'"
---

<objective>
Convert lib/timeline.ts from a linear-math implementation to a pure re-export shim, and update CLAUDE.md with correct stack versions (D-07) and corrected BC dates convention. This is cleanup and documentation — no new logic.

Purpose: lib/timeline.ts currently imports TIMELINE_START, TIMELINE_END, PIXELS_PER_YEAR from data/eras.ts — constants that were deleted in Wave 1. Fixing it makes the project TypeScript-clean end-to-end. The shim preserves backward compatibility until Phase 2 updates its imports. CLAUDE.md corrections prevent future confusion about stack versions.

Output: lib/timeline.ts (shim), CLAUDE.md (corrected), npx tsc --noEmit exits 0 globally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation-yeartopixel/01-CONTEXT.md
@.planning/phases/01-data-foundation-yeartopixel/01-PATTERNS.md
@CLAUDE.md

<interfaces>
From lib/timeline.ts (current, broken state after Wave 1):
- Imports TIMELINE_START, TIMELINE_END, PIXELS_PER_YEAR from '@/data/eras' — these exports no longer exist
- Exports: yearToPixel, pixelToYear, TOTAL_WIDTH, formatYear

Replacement shim — full content to write (from PATTERNS.md):
// lib/timeline.ts — re-export shim (backward compat — Phase 2 will remove this file)
// Math → lib/yearToPixel.ts | Display → lib/yearToDisplay.ts
export { yearToPixel, pixelToYear, TOTAL_WIDTH, TIMELINE_START, TIMELINE_END } from './yearToPixel';
export { yearToDisplay as formatYear } from './yearToDisplay';

From CLAUDE.md — two lines to update:
LINE 1 (stack): "**Stack:** Next.js 16, Motion 12 (useScroll/useTransform), Tailwind v4, next-intl, nuqs, static JSON (v1), Supabase (v2+)"
→ Replace with: "**Stack:** Next.js 14.2, Framer Motion 11 (useScroll/useTransform), Tailwind CSS 3, next-intl 3, nuqs (Phase 6+), static JSON (v1), Supabase (v2+)"

LINE 2 (BC dates): "**BC dates** stored as plain INTEGER (negative = BC, 0 = 1 BC). Never use JS `Date` objects or `Intl.DateTimeFormat` for historical years."
→ Replace with: "**BC dates** stored as plain INTEGER (negative = BC, direct negation: -52 = 52 BC, -1 = 1 BC). Never use JS `Date` objects or `Intl.DateTimeFormat` for historical years. Year 0 is a display-only edge case handled in `yearToDisplay()` — do not store 0 in show data."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace lib/timeline.ts with re-export shim</name>
  <files>lib/timeline.ts</files>
  <read_first>
    - lib/timeline.ts — read current broken content before overwriting
    - lib/yearToPixel.ts — confirm exports: yearToPixel, pixelToYear, TOTAL_WIDTH, TIMELINE_START, TIMELINE_END all exist
    - lib/yearToDisplay.ts — confirm yearToDisplay is exported
  </read_first>
  <action>
    Overwrite lib/timeline.ts with the re-export shim. The file MUST contain exactly:

    A two-line comment header explaining the shim purpose, followed by two export statements:
    - `export { yearToPixel, pixelToYear, TOTAL_WIDTH, TIMELINE_START, TIMELINE_END } from './yearToPixel';`
    - `export { yearToDisplay as formatYear } from './yearToDisplay';`

    The alias `yearToDisplay as formatYear` preserves backward compatibility: any code that imports `formatYear` from `@/lib/timeline` will continue to work — it gets `yearToDisplay` with the legacy name.

    Do NOT include `PIXELS_PER_YEAR` in the shim — that constant is removed entirely. Any Phase 2+ code that imported it must update its own imports (Phase 2 scope, not Phase 1).

    The file should have NO implementation logic — only re-export statements. No function bodies, no constants defined here.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with zero errors (the lib/timeline.ts TypeScript errors are resolved)
    - lib/timeline.ts contains `from './yearToPixel'` (grep confirms)
    - lib/timeline.ts contains `from './yearToDisplay'` (grep confirms)
    - lib/timeline.ts does NOT contain `function yearToPixel` or `PIXELS_PER_YEAR` (grep returns no match)
    - lib/timeline.ts line count is under 10 lines (wc -l or read file to verify brevity)
    - `npm run test:run` still exits 0 (shim didn't break test suite)
  </acceptance_criteria>
  <done>lib/timeline.ts is a pure re-export shim. TypeScript is globally clean. All existing import paths through @/lib/timeline continue to work.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md with correct stack versions and BC dates convention (D-07)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md — read the FULL file before editing; locate the two lines to change:
      1. The "**Stack:**" line in the "## Project" section
      2. The "**BC dates**" line in the "## Critical Constraints" section
  </read_first>
  <action>
    Make exactly two targeted edits to CLAUDE.md (per D-07):

    EDIT 1 — Stack versions line:
    Find: `**Stack:** Next.js 16, Motion 12 (useScroll/useTransform), Tailwind v4, next-intl, nuqs, static JSON (v1), Supabase (v2+)`
    Replace with: `**Stack:** Next.js 14.2, Framer Motion 11 (useScroll/useTransform), Tailwind CSS 3, next-intl 3, nuqs (Phase 6+), static JSON (v1), Supabase (v2+)`

    EDIT 2 — BC dates constraint line:
    Find: `**BC dates** stored as plain INTEGER (negative = BC, 0 = 1 BC). Never use JS \`Date\` objects or \`Intl.DateTimeFormat\` for historical years.`
    Replace with: `**BC dates** stored as plain INTEGER (negative = BC, direct negation: -52 = 52 BC, -1 = 1 BC). Never use JS \`Date\` objects or \`Intl.DateTimeFormat\` for historical years. Year 0 is a display-only edge case handled in \`yearToDisplay()\` — do not store 0 in show data.`

    No other changes to CLAUDE.md. Do not modify the Phases table, Architecture Quick Reference, State Ownership section, or GSD Workflow section.
  </action>
  <verify>
    <automated>grep "Next.js 14.2" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Next.js 14.2" CLAUDE.md` returns a match
    - `grep "Next.js 16" CLAUDE.md` returns NO match (old version removed)
    - `grep "direct negation: -52 = 52 BC" CLAUDE.md` returns a match
    - `grep "0 = 1 BC" CLAUDE.md` returns NO match (old wording removed)
    - `grep "Framer Motion 11" CLAUDE.md` returns a match
    - `grep "Tailwind CSS 3" CLAUDE.md` returns a match
    - CLAUDE.md still contains "**Stack:**" (not renamed or reformatted)
  </acceptance_criteria>
  <done>CLAUDE.md reflects actual installed stack (Next.js 14.2, Framer Motion 11, Tailwind CSS 3, next-intl 3) and correct BC dates convention. The "0 = 1 BC" wording that contradicted D-05 direct negation is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation update | No external I/O — pure file edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | lib/timeline.ts shim re-exports | accept | Shim only re-exports from trusted local modules (./yearToPixel, ./yearToDisplay). No third-party or user-controlled re-exports. |
</threat_model>

<verification>
Final phase gate — run ALL of these after both tasks complete:
1. `npx tsc --noEmit` — exits 0, zero errors
2. `npm run test:run` — exits 0, all tests pass (yearToPixel + yearToDisplay suites)
3. `grep "Next.js 14.2" CLAUDE.md` — returns a match
4. `grep "direct negation: -52 = 52 BC" CLAUDE.md` — returns a match
5. `grep -c "id: '" data/shows.ts` — returns 30
6. `grep -c "pixelsPerYear" data/eras.ts` — returns 9
</verification>

<success_criteria>
Wave 4 complete (Phase 1 complete) when:
- npx tsc --noEmit exits 0 globally
- npm run test:run exits 0
- lib/timeline.ts is a 6-line re-export shim
- CLAUDE.md has correct stack versions and BC dates convention
- All Phase 1 success criteria from ROADMAP.md are met:
  1. yearToPixel and pixelToYear are inverse-verified (test suite passes)
  2. Non-linear scale: renaissance denser than antiquity (test passes)
  3. data/eras.ts defines all 9 eras with pixelsPerYear; boundaries derived from data/eras.ts only
  4. 30 shows spanning -1200 to 2005, TypeScript-clean
  5. BC convention documented in schema; yearToDisplay handles -1, 0, -3000 correctly
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-yeartopixel/01-05-SUMMARY.md` when done.
</output>
