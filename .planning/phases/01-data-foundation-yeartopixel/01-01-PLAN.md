---
phase: 01-data-foundation-yeartopixel
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.mts
  - lib/yearToPixel.test.ts
  - lib/yearToDisplay.test.ts
  - package.json
autonomous: true
requirements:
  - TL-02

must_haves:
  truths:
    - "npm run test:run exits 0 with all stub tests passing"
    - "`@/data/eras` import resolves without error inside test files"
    - "vitest.config.mts loads without parse errors"
  artifacts:
    - path: "vitest.config.mts"
      provides: "Vitest configuration with tsconfigPaths + react plugins"
      contains: "tsconfigPaths()"
    - path: "lib/yearToPixel.test.ts"
      provides: "Test stub for yearToPixel — real assertions added in Wave 2"
      exports: []
    - path: "lib/yearToDisplay.test.ts"
      provides: "Test stub for yearToDisplay — real assertions added in Wave 2"
      exports: []
  key_links:
    - from: "vitest.config.mts"
      to: "tsconfig.json"
      via: "vite-tsconfig-paths plugin reads tsconfig paths automatically"
      pattern: "tsconfigPaths\\(\\)"
    - from: "lib/yearToPixel.test.ts"
      to: "@/data/eras"
      via: "import resolved by vite-tsconfig-paths"
      pattern: "from '@/data/eras'"
---

## Phase Goal

**As a** developer, **I want to** run `npm run test:run` and see green output, **so that** I can trust the test infrastructure before writing any real implementation.

<objective>
Install Vitest and prove the test infrastructure works end-to-end. This is the Walking Skeleton: stub test files that import `@/` aliased modules and pass — confirming Vitest, vite-tsconfig-paths, and vitest.config.mts all integrate correctly with Next.js 14.

Purpose: All subsequent waves depend on a working test runner. Without this skeleton, there is no way to verify that the piecewise yearToPixel implementation is correct.

Output: vitest.config.mts, lib/yearToPixel.test.ts (stubs), lib/yearToDisplay.test.ts (stubs), package.json test scripts, installed devDependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-foundation-yeartopixel/01-CONTEXT.md
@.planning/phases/01-data-foundation-yeartopixel/01-RESEARCH.md
@.planning/phases/01-data-foundation-yeartopixel/01-PATTERNS.md
@.planning/phases/01-data-foundation-yeartopixel/01-SKELETON.md

<interfaces>
From package.json — current scripts and devDependencies:
- scripts: dev, build, start, lint, enrich
- devDependencies include: typescript 5.5.4, tsx 4.17.0, tailwindcss 3.4.10
- dependencies include: next 14.2.29, framer-motion 11.3.31

From tsconfig.json — path alias in effect:
- "paths": { "@/*": ["./*"] } — maps @/ to project root
- "moduleResolution": "bundler"

From data/eras.ts — currently exported (stubs need this import):
- export const ERAS: HistoricalEra[] (9 entries)
- export function getEraForYear(year): HistoricalEra | undefined
- Note: ERAS currently lacks pixelsPerYear field — stubs must not call yearToPixel (which doesn't exist yet)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and create vitest.config.mts</name>
  <files>package.json, vitest.config.mts</files>
  <read_first>
    - package.json — read current scripts and devDependencies before modifying
    - tsconfig.json — confirm "paths": { "@/*": ["./*"] } is present (vite-tsconfig-paths reads this)
  </read_first>
  <action>
    Run `npm install -D vitest @vitejs/plugin-react jsdom vite-tsconfig-paths` from the project root. This adds four devDependencies: vitest ^4.x, @vitejs/plugin-react ^6.x, jsdom ^29.x, vite-tsconfig-paths ^6.x.

    Add two scripts to package.json `scripts` section (after the existing `enrich` entry):
    - "test": "vitest"
    - "test:run": "vitest run"

    Create vitest.config.mts at the project root with this exact content (import order matters — tsconfigPaths must come before react):
    - Import `defineConfig` from 'vitest/config'
    - Import `react` from '@vitejs/plugin-react'
    - Import `tsconfigPaths` from 'vite-tsconfig-paths'
    - Export `defineConfig({ plugins: [tsconfigPaths(), react()], test: { environment: 'node' } })`

    The `environment: 'node'` default applies to Phase 1 pure-math tests. Component tests in Phase 3+ will use `// @vitest-environment jsdom` per file instead of changing this global setting.

    Do NOT add a `globals: true` setting — test files must use explicit `import { describe, it, expect } from 'vitest'` (per D-12 and the established pattern in PATTERNS.md).
  </action>
  <verify>
    <automated>npx vitest run --version</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run` script exists in package.json (grep "test:run" package.json returns a match)
    - `npm test` script exists in package.json (grep '"test":' package.json returns a match)
    - vitest.config.mts exists at project root with `tsconfigPaths()` before `react()` in plugins array
    - `npx vitest run --version` exits 0 and prints a version number starting with "4."
    - package.json devDependencies contains "vitest", "@vitejs/plugin-react", "jsdom", "vite-tsconfig-paths"
  </acceptance_criteria>
  <done>Vitest is installed, vitest.config.mts exists with correct plugin order, and both test scripts are in package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Write passing test stubs for yearToPixel and yearToDisplay</name>
  <files>lib/yearToPixel.test.ts, lib/yearToDisplay.test.ts</files>
  <read_first>
    - .planning/phases/01-data-foundation-yeartopixel/01-RESEARCH.md — section "Unit Test Structure" for the full test shapes
    - .planning/phases/01-data-foundation-yeartopixel/01-PATTERNS.md — section "lib/yearToPixel.test.ts" and "lib/yearToDisplay.test.ts" for exact import patterns
    - data/eras.ts — confirm ERAS export name (the stub will import it to verify @/ alias resolves)
    - lib/yearToDisplay.ts — check if it already exists (it does not yet; stub must not import from it)
  </read_first>
  <action>
    Create `lib/yearToPixel.test.ts` as a stub. The stub MUST:
    - Import `describe`, `it`, `expect` from 'vitest' (named imports, no globals)
    - Import `ERAS` from '@/data/eras' — this validates that the @/ alias resolves in Vitest
    - Define a `describe('yearToPixel — stubs (Wave 0)', () => { ... })` block
    - Inside: one `it('infrastructure: @/ alias resolves and ERAS loads', () => { expect(ERAS.length).toBeGreaterThan(0) })` — a real assertion that will pass because ERAS currently has 9 entries
    - Add commented-out `it.todo` entries for each real test case from D-13: round-trip inverse, era boundaries, year -3000, BC/AD crossing, year 0. Use `it.todo('maps TIMELINE_START (-3000) to 0px')` etc. These become real tests in Wave 2 (01-03).

    Create `lib/yearToDisplay.test.ts` as a stub. The stub MUST:
    - Import `describe`, `it`, `expect` from 'vitest'
    - Define a `describe('yearToDisplay — stubs (Wave 0)', () => { ... })` block
    - Inside: one `it('infrastructure: vitest runs in node environment', () => { expect(typeof process).toBe('object') })` — passes unconditionally
    - Add `it.todo` entries for each yearToDisplay test case from D-13 and PATTERNS.md: FR/EN BC formatting, year 0 guard, year -3000, positive years, default locale. Use `it.todo('formats -52 as "52 av. J.-C." in French')` etc.

    Do NOT import from `lib/yearToPixel` or `lib/yearToDisplay` — those files do not exist yet and would cause import errors. The stubs only import from existing modules.

    Do NOT use `describe.skip` or `it.skip` — use `it.todo` for unimplemented cases. `it.todo` cases do not run and do not cause failures.
  </action>
  <verify>
    <automated>npx vitest run lib/yearToPixel.test.ts lib/yearToDisplay.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/yearToPixel.test.ts lib/yearToDisplay.test.ts` exits 0
    - Output shows "1 passed" for yearToPixel.test.ts (the @/ alias assertion passes)
    - Output shows "1 passed" for yearToDisplay.test.ts (the node environment assertion passes)
    - Output shows todo entries listed (not failing) — Vitest prints todos as "todo" not as failures
    - No "Cannot find module '@/data/eras'" error
    - No "Cannot find module 'vitest'" error
    - lib/yearToPixel.test.ts contains `import { ERAS } from '@/data/eras'` (grep confirms)
    - lib/yearToDisplay.test.ts contains `import { describe, it, expect } from 'vitest'` (grep confirms)
  </acceptance_criteria>
  <done>Both stub test files exist, run without errors, and pass their skeleton assertions. The @/ alias resolves correctly inside Vitest. All real test cases are registered as it.todo for Wave 2 implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependencies | Four new packages installed from npm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm install (vitest, @vitejs/plugin-react, jsdom, vite-tsconfig-paths) | mitigate | Package Legitimacy Audit in RESEARCH.md verified all four packages: vitest approved (Evan You / Anthony Fu, official Next.js docs), @vitejs/plugin-react approved, vite-tsconfig-paths approved, jsdom approved. No [SLOP] packages. |
</threat_model>

<verification>
After both tasks complete:
1. `npm run test:run` exits 0
2. Output contains "2 test files" or similar, all passing
3. No TypeScript errors: `npx tsc --noEmit` (may show TS errors for yearToPixel.ts not existing yet — acceptable at Wave 0 since the test stubs do not import non-existent modules)
</verification>

<success_criteria>
Walking Skeleton is complete when:
- `npm run test:run` exits 0
- vitest.config.mts exists with plugins: [tsconfigPaths(), react()]
- lib/yearToPixel.test.ts and lib/yearToDisplay.test.ts both exist with it.todo entries for Wave 2
- @/ alias resolves inside Vitest test files (proven by the ERAS.length assertion passing)
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-yeartopixel/01-01-SUMMARY.md` when done.
</output>
